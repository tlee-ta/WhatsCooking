<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_meal_item"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_margin="4dp"
        android:contentDescription="@string/content_des_meal_image"
        android:scaleType="centerCrop"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_text_bg"
        android:layout_width="0dp"
        android:layout_height="60dp"
        app:srcCompat="@drawable/photo_background_protection"
        app:layout_constraintBottom_toBottomOf="@+id/iv_meal_item"
        app:layout_constraintEnd_toEndOf="@+id/iv_meal_item"
        app:layout_constraintStart_toStartOf="@+id/iv_meal_item"
        android:contentDescription="@string/text_bg" />

    <TextView
        android:id="@+id/tv_meal_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="2dp"
        android:layout_marginStart="2dp"
        android:layout_marginEnd="2dp"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:textStyle="bold"
        android:ellipsize="end"
        android:maxLines="2"
        app:layout_constraintBottom_toBottomOf="@id/iv_text_bg"
        app:layout_constraintEnd_toEndOf="@+id/iv_text_bg"
        app:layout_constraintStart_toStartOf="@+id/iv_text_bg" />

</androidx.constraintlayout.widget.ConstraintLayout>